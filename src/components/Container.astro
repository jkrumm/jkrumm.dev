---
interface Props {
	width?: 'sm' | 'md' | 'lg' | 'xl'
}

const { width } = Astro.props

const WIDTHS = {
	sm: 'max-w-screen-sm',
	md: 'max-w-screen-md',
	lg: 'max-w-screen-lg',
	xl: 'max-w-screen-xl'
}
---

<div class={`mx-auto ${WIDTHS[width ? width : 'md']} px-5`}>
	<slot />
</div>
